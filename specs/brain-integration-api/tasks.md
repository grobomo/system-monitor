# Tasks: Brain Integration API

- [ ] T023: GET /api/summary — aggregate repeat offenders + anomalies from ring buffer
- [ ] T024: GET /api/health — liveness check with uptime, event count, last event timestamp
- [ ] T025: Command normalizer — group events by normalized command (strip PIDs, temp paths, quotes)
