# Tasks: IOC Monitor

- [ ] T015a: IOC data structures — IocEvent, IocSeverity, event ID registry
- [ ] T015b: wevtutil query wrapper — query Security/System logs, parse XML output
- [ ] T015c: CLI `system-monitor ioc` command — show recent IOCs with filters
- [ ] T015d: Dashboard integration — /api/iocs endpoint + IOC section in dashboard HTML
- [ ] T015e: Guard integration — run IOC polling alongside focus-steal detection
